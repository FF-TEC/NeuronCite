"""Shared pytest configuration for the NeuronCite client test suite.

This file serves as a pytest package marker so that test discovery works
correctly when running ``pytest`` from the repository root or from the
``clients/python/`` directory.
"""
